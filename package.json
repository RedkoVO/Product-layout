{
  "name": "ProductLayout",
  "private": true,
  "version": "1.0.0",
  "description": "Product Layout",
  "repository": "https://github.com/RedkoVO",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
